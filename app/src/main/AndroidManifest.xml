<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uillover.taskfire">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TaskFireApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskFire"
        tools:targetApi="24">

        <!-- Activité principale (Ajoutée et corrigée) -->
        <activity
            android:name=".tasks.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- Écran d'authentification -->
        <activity
            android:name=".auth.AuthActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activité d'ajout de tâche -->
        <activity
            android:name=".tasks.AddTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskFire.NoActionBar"
            android:parentActivityName=".tasks.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".tasks.MainActivity" />
        </activity>

        <!-- Activité de détails de tâche -->
        <activity
            android:name=".tasks.TaskDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskFire.NoActionBar"
            android:parentActivityName=".tasks.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".tasks.MainActivity" />
        </activity>

        <!-- Activité du profil utilisateur -->
        <activity
            android:name=".profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.TaskFire.NoActionBar"
            android:parentActivityName=".tasks.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".tasks.MainActivity" />
        </activity>

        <!-- Service Firebase pour les notifications -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
